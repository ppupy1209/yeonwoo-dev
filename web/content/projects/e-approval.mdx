---
title: 전자 결재 시스템
slug: e-approval
order: 4
summary: 기안자가 결재를 상신하고, 상급자가 모바일 앱에서 승인·반려하는 전자 결재 시스템
role: 백엔드 개발
stack: [Java, Spring, MySQL]
metric:
  label: Lost Update 문제 해결
---

## 개요
- 기안자가 결재를 상신하고 승인권자가 모바일에서 승인·반려 처리하는 전자 결재 시스템
- 동시에 들어온 결재 처리 요청의 정합성 보장
- 결재선 전체 엔진보다 승인·반려 상태 전이의 동시성 제어에 범위 집중

## 문제

- 여러 승인권자의 동시 처리로 먼저 반영된 승인·반려 결과가 덮어써질 가능성
- 결재 상태 확인과 상태 변경이 분리될 경우 Lost Update 발생 가능
- 모바일 중복 터치, 네트워크 재시도, 대리 처리 요청이 겹칠 때 낮은 빈도지만 높은 영향도

## 해결 전략

- 현재 결재 상태 조건을 포함한 단일 UPDATE 사용
- UPDATE affected rows가 0이면 이미 처리된 결재로 판단
- 충돌 상황을 비즈니스 예외로 처리해 중복 승인·반려 차단
- 결재선 순차·병렬 처리 규칙과 상태 전이 충돌 제어를 분리

## 기술 선택 이유

- 조건부 UPDATE
  - 충돌 빈도가 높지 않은 결재 처리 흐름에 적합
  - 비관적 락보다 Lock 점유 시간과 대기 비용 감소
  - `WHERE status = 'WAITING'` 조건으로 DB의 원자적 갱신 결과 활용

- affected rows 검증
  - 별도 조회 없이 UPDATE 성공 여부로 동시성 충돌 판단 가능
  - DB가 보장하는 원자성을 애플리케이션 로직에 직접 활용

- Version 기반 낙관적 락 제외
  - 별도 version 컬럼 추가와 예외 변환 비용 대비 상태 전이 조건만으로 충분
  - 결재 상태 변경 대상이 단일 row라 조건부 UPDATE가 더 단순

## 검증

### 테스트 설정
- 동일 결재 건에 승인 요청과 반려 요청 동시 전송
- 먼저 도착한 요청만 성공 처리되는지 확인
- 후속 요청의 affected rows 0 반환과 예외 처리 확인
- 모바일 재요청과 중복 클릭 상황을 같은 결재 ID로 재현

### 측정 결과
- Lost Update 재현 시나리오 차단
- 처리 완료 결재에 대한 중복 상태 변경 방지
- 낮은 빈도의 충돌도 승인 결과 오염으로 이어지지 않는지 확인

## 배운 점
- 비관적 락과 version 컬럼을 제외한 이유를 상태 전이 범위로 설명 가능
- 결재 도메인의 복잡성과 이번 개선 범위를 분리해 말하는 편이 더 안전
